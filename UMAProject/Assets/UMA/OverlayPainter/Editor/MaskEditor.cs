namespace UMA.TexturePaint.Editor
{
    // The former global/structural mask editor was intentionally removed. Per-layer editable
    // masks now live in the layer row menu and the shared layer-effects popup.
    internal static class MaskEditor
    {
    }
}
